# Prefer NVIDIA proprietary/open driver over nouveau on hosts with supported NVIDIA GPUs.
# This avoids Hyprland EGL/DRM crashes seen with nouveau on desktop NVIDIA hardware.
blacklist nouveau
options nouveau modeset=0
options nvidia_drm modeset=1 fbdev=1
